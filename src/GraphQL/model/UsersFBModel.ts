import {PageFBModel} from "."
export type UsersFBModel = {
    id: number;
    name: string;
    id_FB: string;
    user_TOKEN: string;
    created_at : Date;
    updated_at : Date;

    // Foreign keys

  };